# Roadmap — Stay on Log

Беклог, приоритеты и дизайн-заметки. Контекст для продолжения работы в новой сессии.
Это карта намерений; **что реально реализовано — смотри в коде** (`js/`).

Сквозной принцип: **сначала честная и приятная одиночная сессия → социалка → экономика →
дистрибуция.** Скины без ретеншна не окупаются.

## Уже сделано
- Сплит на ES-модули, деплой на Vercel из `main`.
- Управление наклоном; порог падения **±110°**; двухступенчатое сглаживание инпута.
- Заглушка на десктопе + скрытый dev-режим `?dev=1` (клавиатура + слайдеры тюнинга).
- Онбординг, событийный scoring, синтез-SFX, вибрация, mute.
- **Этап 1 «фундамент» (июль 2026):**
  - Палитро-система на CSS-переменных (`@property` → плавные кроссфейды градиентов).
  - **Биомы по времени**: день → закат (40с) → ночь (80с) → шторм (140с); солнце/луна,
    облака, звёзды, молнии; фазы сложности стали видимыми.
  - Богатая вода (3 слоя волн, пена, блики), новый арт бревна (кора, живые кольца),
    персонаж с лицом и выражениями (прыжок/удар — чистый CSS на классах `.jumping`/`.hit`).
  - **3 типа препятствий**: сучок с мхом (любой прыжок), высокая ветка (прыжок в средней
    фазе — паника в последний момент = удар), двойной сучок (ритм, гэп зависит от скорости).
  - **Честный телеграф**: стрелка со стороны подхода; реверс при активном препятствии
    конвертируется в смену скорости; при активном double скорость заморожена.
  - Пейсинг: грейс 25с → **12с**, кулдаун препятствий ×1/×0.7/×0.5 по фазам.
  - **Комбо** ×2..×5 к очкам за серию, попап «+50 ×2», комбо-звук, «вжух» смены биома.
  - HUD-переверстка: верхний бар сеткой (жизни/счёт/кнопки), пилюля направления+скорости
    над водой, `100dvh` + safe-area (лечит наезд UI на бревно в TG/Safari).
- **Этап 1.1 «правки по плейтесту» (готово):**
  - **F1 — плавный прыжок**: медленнее (~620ms) и параболичнее, чем резкий прежний хоп
    (`css/styles.css` keyframes + `js/config.js` `JUMP_DURATION`).
  - **F2 — звук/вибрация**: надёжная разблокировка `AudioContext` на каждом жесте (не только
    Start), ленивая инициализация Telegram `HapticFeedback` (`js/audio.js`, `js/haptics.js`).
  - **F3 — camera-lock эксперимент (испробован и откачен)**: человечек визуально закреплён в
    астрономическом верху бревна, мир (вода/горизонт) кренится вместо него — идея была сделать
    отклонение интуитивнее читать. На плейтесте не зашло → откачен `git revert` (коммит 470d782,
    отменяет 16264f4). Игра вернулась к обычной камере (мир статичен, человечек ходит по кругу).
    **Не предлагать эту идею повторно** без явного запроса пользователя.

- **Этап 1.2 «контроль-фил и отдача» (июль 2026, ждёт плейтеста):**
  - **Steering assist («гидроусилитель»)**: игра сама компенсирует долю вращения бревна —
    `ASSIST_PHASE_FACTOR=[0.30, 0.18, 0.10]` по фазам (новичкам легче, поздняя игра скилловая);
    глобальный множитель — слайдер «assist» в `?dev=1` (`stayOnLog_assistMult`).
  - **Danger-виньетка**: красное свечение края экрана со стороны сноса, от `DANGER_WARN_FROM=55°`
    до полной яркости у порога падения — телеграф «почему я упал».
  - **Процедурная музыка** (Web Audio, без ассетов): бас+арпеджио+пад, lookahead-планировщик;
    настроение (BPM/лад/плотность) следует биому: день 96bpm мажор → шторм 116bpm минор.
    mp3-путь удалён; unmute теперь возвращает музыку сразу.
  - **Хаптики на позитив**: клир/комбо, тики отсчёта 3-2-1, смена биома, «новый рекорд»
    (раз за забег, не на первом забеге). Ограничение платформы: вне Telegram на iOS вибрации
    невозможны вовсе (нет API в Safari) — внутри TG работает `HapticFeedback`.

- **Этап 2 «TG-нативность без бэкенда» (июль 2026, ждёт плейтеста):**
  - **Личность из TG**: ник по умолчанию из `initDataUnsafe.user` (first_name/username) — внутри
    Telegram оверлей «как тебя зовут?» больше не показывается. Приоритет: введённый вручную >
    CloudStorage > TG-профиль (см. `nameSource` в game.js). Весь доступ к SDK — через новый `js/tg.js`.
  - **Рекорд и ник в `CloudStorage`** (клиенты ≥6.9, гард версии): async-merge на загрузке —
    больший рекорд побеждает, локальное всегда работает мгновенно; пуш в облако на новом рекорде
    и при смене ника.
  - **Кнопка «Похвастаться»** (`#share-btn`, видна при рекорде > 0): Telegram `t.me/share/url` →
    Web Share API → буфер обмена («Скопировано!»). Пока шарится прямой URL (`SHARE_URL` в
    config.js) — deep-link `t.me/<bot>/<app>?startapp=…` требует бота, которого нет до этапа 3.
  - **Клавиатурный режим на десктопе**: в заглушке кнопка «🎹 Играть с клавиатурой» (←/→, пробел) —
    расшаренные ссылки в Desktop Telegram/браузере больше не тупик; `?dev=1` теперь отвечает
    только за панель тюнинга.
  - **Нормализация delta-time** (закрыт блокер качества из этапа 6): скорости = «градусы за кадр
    60 Гц» на любом дисплее (dtF в gameLoop, клэмп 50мс); `state.elapsed` копится по живым
    кадрам — свёрнутое приложение не фармит очки выживания и не прыгает по фазам/биомам.
    На 60 Гц поведение идентично прежнему; если темп на устройстве заметно упал — экран был
    90/120 Гц: перетюнить `MIN_SPEED`/`MAX_SPEED`, **не** revert.

- **Волна 1 «Понятное ядро» (5 июля 2026, ждёт плейтеста)** — итог deep-интервью:
  ядро не считывалось новичком (штурвал непривычен, тап-прыжок непонятен, «никто не
  попросил ссылку»); метрика успеха отныне — возвращаемость.
  - **Схема управления B «наклон-руль»** (`stayOnLog_controlScheme='tilt'`): удержание
    наклона → скорость докрутки (`TILT_*` в config.js), интеграция только в `gameLoop`
    через `dtF`, калибровка нуля в конце отсчёта, посев первого сэмпла в `input.js`.
    Схема A (штурвал) — дефолт и не тронута. Переключатель + 4 слайдера в `?dev=1`;
    пользовательский переключатель в howto — после плейтеста.
  - **Коуч-баннеры новичка**: «ТАПНИ — ПРЫЖОК!» (первые `HINT_JUMP_RUNS=3` забега, до
    первого прыжка) и «⟲ КРУТИ/НАКЛОНЯЙ ВЛЕВО» при сносе (первые 5 забегов, гистерезис
    + кулдаун 6с; на десктопе «ЖМИ»). Ключ `stayOnLog_runCount`.
  - **Интерактивный туториал** (первый старт или `?tut=1`): медленное бревно, elapsed
    заморожен (не спавнит, не меняет направление, не копит очки) → удержись 4с в ±45° →
    перепрыгни принудительный сучок (`forceEmerge`, hit без урона, падение = мягкий
    контр-поворотный ресет) → «Готов!» → штатный забег. Кнопка «Пропустить»; флаг
    `stayOnLog_seenTutorial_v1` пишется на финише/скипе. Автопоказ howto ретайрнут
    (оверлей остался за кнопкой `?`).
  - Дальше по программе интервью: **монеты на бревне + скины → миссии → лидерборд**.
  - **Бот создан (5 июля 2026): `@StayOnLog_bot`, Mini App `t.me/StayOnLog_bot/game`** —
    `SHARE_URL` уже шарит deep-link. Токен бота — у владельца, понадобится только на этапе 3
    (env Vercel, серверная валидация `initData`). В репо токен не попадает никогда.

- **Волна 2 «Стрелка v2 + монеты + магазин + лидерборд» (ночь 5→6 июля 2026, ждёт
  плейтеста)** — по deep-интервью вечера 5 июля:
  - **Стрелка v2 на бревне** (`js/dirarrow.js`, дизайн владельца): основная дуга = куда
    крутится, длина 40–150° = скорость; превью-дуга = что сделает СЛЕДУЮЩАЯ смена
    (`state.pendingChange`, ролл заранее с капами по времени срабатывания); варнинг-мигание
    за 1.5с и промоушен-морф превью→основная в момент смены. Пилюля направления ретайрнута;
    «УПАЛ! Очки» переехал в hud-center (заодно починен латентный баг: строка результата была
    невидима внутри скрытой пилюли). Новый шаг туториала «пойми стрелку» со скриптовым
    реверсом; музыка в туториале включена (багфикс).
  - **Смягчение фазы 3** («бешеное бревно» после ~200 очков): запрет высокоскоростных
    реверсов распространён на фазу 3, интервалы смен ×1.3, assist 0.15→0.18, speed-ramp
    (`SPEED_RAMP_K`) + провал скорости на реверсе (`REVERSAL_SPEED_DIP`).
  - **Смерть с объяснением** (`js/quips.js`): забавная строка «почему упал» по причине
    (fall/knot/branch/double) с контекстными приоритетами (смена/шторм/скорость).
  - **Монеты на бревне** (`js/coins.js`): стейт-машина как у препятствий; обычная (1 🪙,
    контакт) и парящая (2 🪙, только в прыжке, 35%); не выныривает при подходящем
    препятствии; кошелёк в HUD, «+N 🪙» на результате, `stayOnLog_coins` (локально).
  - **Магазин 🛒** (`js/shop.js`): «Твоё бревно» — аватарка из TG клипом на торце (300 🪙,
    премиум; вне TG под замком), «Запасное сердце» (25 🪙 → +1 hp на следующий забег);
    скины синкаются с CloudStorage (union).
  - **Revive** «⚡ Продолжить за 40 🪙» на экране смерти (раз за забег): тот же
    score/elapsed/combo/биом, hp=1, щит 1.8с, быстрый отсчёт, свежие препятствия.
  - **Лидерборд**: `api/score.js`+`api/top.js` (Vercel serverless, zero-deps, HMAC-валидация
    initData, Upstash `ZADD GT`); клиент `js/leaderboard.js` — кнопка 🏆 появляется только
    если бэк жив, топ-10 с медалями, свой ранг. **Осталось от владельца: 5 минут по
    [`BACKEND_SETUP.md`](BACKEND_SETUP.md)** (Upstash + `TG_BOT_TOKEN` в env + redeploy);
    до этого /api отвечает 503 и фича скрыта.

- **Волна 3 «Кампания» (6 июля 2026, плейтест идёт — правки обновы продолжаются)** —
  итог deep-интервью 6 июля («вау-гэп»: биомы = хребет игры —
  путешествие+прогрессия+миссии+боссы):
  - **Цикл суток вместо вечного шторма**: полдень→закат→ночь→шторм→рассвет→полдень
    (`DAY_CYCLE_MS=240с`); сложность читает сырой `elapsed` (после первого круга — максимум),
    палитра крутится по кругу; wrap = бейдж «🌞 СУТКИ! Биом пройден» + запись в прогресс.
    Ключевой ход рефактора: механика сложности не тронута ни байтом.
  - **Кампания**: биом на весь забег (выбор на карте), **3 обязательных задания** на переход
    (кумулятивные счётчики, за монеты не покупаются; скип за донат — потом), тост «✅ задание
    выполнено» в забеге и строка на экране смерти. Персист `stayOnLog_campaign_v1`
    (localStorage + CloudStorage **max-merge** — монотонные счётчики не дюпают).
  - **Боссы в шторм-фазе каждого цикла** (js/boss.js): Земля — 🦈 акула-**паттерн**
    (7 телеграфированных атак: наскок на сторону бревна — уйди на другую; перекус поверху —
    прыгай в момент), Зимний — 🐋 косатка-**погоня** (мир ускоряется ×1.3, снапы-прыжки,
    выживи 35с). Форшадоуинг: плавники в воде с первой секунды. Победа = +150 очков + квест.
    Хиты через общий hp/инвулн; во время боя препятствия/монеты не выныривают, смены
    направления откладываются (паттерн double); revive продолжает бой с телеграфа.
  - **Карта кампании 🗺**: одна линия, главы по 4 карточки, всё видно с первого дня —
    Стихии (Земля→Зимний→Лавовый→Космос) → Цивилизации → Смыслы → Ритм (Диско/Рок/Рэп/
    Классика) → Детство (Игрушки/Цирк/Кубический/Мультяшный) → Искусство → Спорт → Страны
    мира (арка регионов); «?»-карточки и «скоро»-анонсы; секция ⚽ События — слот.
  - **Зимний мир целиком**: 5 палитр `biome-w-*` (полярная ночь, метель), обмёрзшее бревно
    (переменные `--bark-*/--wood-*` — вынесены в этой же волне), сучок→ледяной бугор,
    ветка→сосулька (CSS-only, хитбоксы нетронуты), 5 мудов музыки, свои 3 задания;
    анлок — все задания Земли.
  - Dev-кнопки `?dev=1`: «→шторм» (варп к боссу) и «анлок» (закрыть все квесты).

  - **Плейтест-правки волны 3 (6–7 июля, в процессе)** — первый заход на реальном
    телефоне поймал баг-волну и арт-нотки; фиксится и тюнится по ходу, обнова ещё
    не закрыта:
    1. **Критический фикс «невидимые боссы / гигантский силуэт на старте»**
       (5428ec9, aa60701): в CSS-комментарии стояло буквальное «tele-* / strike-*»
       (без пробела в реальном коде!) — пара `*/` в такой записи досрочно
       закрывала комментарий, и парсер выбрасывал базовое правило `#boss-shark`
       целиком. Без базы: на старте дети акулы растягивались на весь `#boss-layer`
       (полноэкранный силуэт с зубами), в бою — схлопывались в 0px. Плюс страховка
       `#boss-layer:not(.on) > * { display:none !important }` и кэш-бастер `?v=N`
       на `styles.css` (вебвью Telegram держит стейл-CSS сквозь `max-age=0`).
    2. **Плавники — сидят в воде** (0452410, 0f63fc8): раньше их обрезал
       `overflow:hidden` контейнера воды до 4px «полосочек» над гребнем волны;
       теперь целиком внутри контейнера — силуэт спинного плавника через
       clip-path + пенный кильватер, затем отзеркалены (крутая передняя кромка
       ведёт по ходу движения, было «задом наперёд»).
    3. **Рисованный SVG-арт боссов — новая арт-планка** (717d3ba): схематичные
       дивы заменены на inline-SVG `#shark-svg`/`#orca-svg` в позе вертикального
       прыжка, пасть к бревну — акула с контртенью/зубами/жабрами/плавниками,
       косатка с глазным пятном/седлом/высоким плавником; вариант переключает
       класс `.orca`.
    4. **Уворот-хореография косатки** (1222a0a): вместо мгновенного наскока —
       три такта: в погоне низко за бревном (голова+плавник над водой), на
       телеграфе ПОГРУЖАЕТСЯ и дрожит-заряжается, на страйке свеча-прыжок до
       кромки бревна с пенным всплеском (`#boss-splash`), `snap-recover` —
       нырок обратно; прыжок игрока теперь читается как уворот от выпада.
    5. **Задания в забеге — карточка вместо панели** (8c0bbbf, затем
       переделано в 1222a0a по фидбеку владельца): сперва постоянная панель
       слева, «колхозно» — заменена аккуратной карточкой `#quest-hud` по
       центру сверху («ЗАДАНИЯ · <биом>» + строки прогресса): 5с на старте
       забега, потом скрыта, на каждом тике всплывает на 2.5с (выполненная
       строка зеленеет «✓»); отдельный тост «задание выполнено» убран —
       дублировал карточку.
    6. **Карта кампании** (9d3ceec): секция ⚽ События поднята на самый верх;
       все главы пронумерованы I–VIII; у ВСЕХ карточек (включая «?») теперь
       есть имена (составы глав II–VIII — черновик агента, данные в
       `CHAPTERS`, владелец может переименовать); тап по замку вместо
       чеклиста показывает бабл «Выполни задания в биоме «…»».
    7. **Льдышки Зимнего** (2be1024): низкое препятствие — полупрозрачный
       кубик льда с бликом, высокое — зазубренный осколок через clip-path;
       геометрия хитбоксов не менялась.
    8. **Парящая монета = 1 🪙** (0f63fc8, решение владельца): раньше 2 —
       прыжок за ней теперь чисто про стиль/квесты, а не двойную выгоду.

  - **Босс-реворк 2.0 (7 июля 2026, итог deep-интервью — закрывает обнову)** —
    владелец: атака косатки не читалась («подныривает лишь немножко», удара по
    стикману нет, «почему прыжок, если атака по центру?»), акула «реализована
    бездарно» (тайминги нравятся — не тронуты ни мс, арт и читаемость — нет):
    1. **Косатка = гигант шире бревна** (`#boss-orca-giant`, 340px, фронтальный
       ракурс снизу, гейп зубов во всю верхушку): живёт СИБЛИНГОМ `#boss-layer`
       на z=1 — за водой и бревном (`isolation:isolate` на `#game-container`
       схлопывает его стек в z=5), нырок под воду прячет её бесплатно. Погоня:
       тень+спинной плавник `#boss-shadow` (z=4) рыщут за бревном; телеграф:
       плавник ныряет, тень сгущается по центру и дрожит; страйк: свеча снизу
       до верхушки, пасть захлопывается на пике, линия смыкания зубов у ног
       стоящего игрока (прыжок чисто уходит над носом); recover: нырок со
       всплеском (`#boss-splash` укрупнён до 300×90 у линии воды); победа —
       прощальный полувылет. Телеграф снапа 1200→1400мс, гвард последнего
       снапа честный (весь такт телеграф+страйк до победного таймера).
    2. **Акула: bite = проезд живого профиля** (`#boss-shark-side`, z=4 за
       бревном/игроком) вдоль верхушки за ровно страйк-окно 600мс — пасть над
       центром точно в миг проверки уворота; стороны чередуются
       (`biteFlip`→класс `bite-rev`, свечение следует за стороной); lunge =
       кейфреймовый бросок пастью в бок бревна с овершутом и клинчем.
       Абстрактный `#boss-jaw` больше не показывается.
    3. **Видимый удар**: стикмана СНОСИТ — группа `.rig` внутри `#player`
       (transform корня занят прыжком), кейфреймы `boss-knock-l/r/up` по
       `bossHitDir()`, большой шейк, плотный burst; фатальный боковой удар
       смещает дугу падения от удара. Реальный `normPos` не трогается —
       двойного наказания нет.
    4. **`docs/ART_PIPELINE.md`**: владелец генерит SVG внешним ИИ по
       тех.требованиям — агент чистит/размечает/анимирует/встраивает.
    5. **Вердикт владельца (7 июля, вечер)**: косатка-гигант СОГЛАСОВАНА;
       акула принята условно — «стоит улучшить в будущем» (арт/анимации
       lunge и проезда докрутить следующей арт-итерацией; кандидат на первый
       заход по docs/ART_PIPELINE.md — сгенерить SVG-референс внешним ИИ).
    Демонтаж мёртвых `#orca-svg`/`#boss-jaw` выполнен после согласования (4cc2063).

- **Плейтест-чеклист волны 3** (на один заход, `?dev=1` ускоряет; актуализирован
  под правки из подсекции выше — обнова ещё не закрыта, список может ещё поменяться):
  1. Бревно и палитры до 140с неотличимы от прежних; темп смен не изменился.
  2. Старт-экран и первые секунды забега: боссов НЕ видно — включая НОВЫЕ сиблинги
     (гигант-косатка, тень с плавником, акула-профиль) — регресс на инцидент
     невидимых/гигантских боссов (страховки `:not(.on)`).
  3. Дожить/доварпать до шторма (Земля): lunge = бросок пастью в бок с клинчем
     читается? Перекус = ПРОЕЗД акулы по верхушке (стороны чередуются, свечение
     следует), прыжок над ней честен? Хит = снос стикмана + большой шейк
     (сердце+инвулн)? Победа — фанфары, +150, квест 1/1.
  4. Рассвет после шторма → полдень на 240с: бейдж «СУТКИ!», музыка мягчеет, сложность не падает.
  5. Задания: карточка `#quest-hud` — успевает ли прочитаться за 5с на старте забега,
     не мешает ли/не закрывает ли важное при всплытии на 2.5с на тике; счётчики
     переживают перезапуск; тап по замку Зимнего на карте — бабл «Выполни задания…»
     показывается и гаснет (чеклиста на карте больше нет — это фича, не баг).
  6. Карта: скроллится (не прыгает!), события ⚽ сверху, все главы I–VIII, у всех
     карточек (включая «?») есть подписи, выбор Земли/Зимнего, «✓ пройден ×N», «скоро».
  7. Зимний (после анлока или дев-кнопки): вся палитра цикла, льдышки читаются как лёд
     (кубик/осколок, не мох/кора), муд холоднее, плавники чёрно-белые и сидят В воде
     (не полосками над гребнем); косатка-ГИГАНТ: в погоне тень+плавник за бревном
     читаются как «она под тобой»? На телеграфе плавник ныряет, тень дрожит по
     центру — понятно, что бить будет СНИЗУ? Страйк — вылет шире бревна с
     захлопыванием пасти, прыжок чисто уходит над носом? Провал = стикмана видимо
     сносит вверх? Победа по таймеру (35с). Перф свечи (гигант+всплеск) на
     телефоне не проседает? На узком экране (~320px) кончики грудных плавников
     режутся терпимо?
  8. Смерть от боссов: квипы акулы/косатки; revive в бою — бой продолжается с телеграфа.
  9. Монеты: парящая теперь = 1 🪙 (не 2) — не читается ли теперь как «невыгодная»,
     не потеряла ли смысл как челлендж.
  10. Регресс: туториал `?tut=1` (всегда Земля, без плавников/босса), магазин/сердце/revive,
      лидерборд, шаринг, схема A.

- **Волна 4 «Битвы чатов MVP» (7 июля 2026, задеплоена — ждёт плейтеста)** — чат против чата:
  окно 24ч, счёт команды = **сумма** очков всех забегов всех участников (массовость и грайнд
  решают, как и задумано в порядке волн 4–6 ниже).
  - **Сервер**: `api/battle.js` — один эндпоинт, op-роутинг `create/join/state/submit/rename`.
    Команды авто по `chat_instance` (создательский чат = A, первый чужой клеймит B через
    `HSETNX`; сторона юзера навсегда; личка/третий чат → ручной выбор стороны). Дельта-дедуп
    по `uid:runId` — revive не даёт двойного счёта. Имя команды пишет первый вошедший
    (`nameSetBy*`), rename доступен только пока команда не сыграла (`score=0`). Ключи `bt:<id>`
    (+`:members/:contrib/:names/:runs`) TTL 8 дней, `bt:u:<uid>` — одна активная битва на
    юзера. `state` кэшируется s-maxage=5. Санити-кап 50000, санитизация имён (24 символа,
    control-символы режутся). `api/_kv.js`: `validateInitData` теперь возвращает
    `{user, chatInstance, chatType, startParam}` (HMAC покрывает все поля); `score.js`
    обновлён под новую сигнатуру.
  - **Клиент**: `js/battle.js` — самоскрытие через `whenBackendAlive` (новый экспорт
    `leaderboard.js` поверх той же единой пробы `/api/top`) И только в Telegram;
    `stayOnLog_activeBattle` в localStorage {id,side,ends,имена,счёт,myTotal,canRename,finished};
    кнопка `⚔️` с бейджем «мои:вражьи»; оверлей `#battle-overlay` с 6 состояниями (создать /
    подтверждение вызова / выбор стороны / имя команды / активный бой с versus-шапкой +
    таймером от серверного `now` + вкладами обеих команд + двумя шарингами
    «⚔️ Вызвать врагов»/«📣 Клич своим» вокруг ОДНОЙ ссылки `SHARE_URL?startapp=b_<id>` /
    финал ПОБЕДА-ПОРАЖЕНИЕ-НИЧЬЯ + «Новая битва»); поллинг `BATTLE_POLL_MS=10000` только при
    открытом оверлее; boot-роутинг `startapp=b_<id>` → подтверждение вступления. Имена — только
    `textContent`.
  - **Прочее**: `js/tg.js` — `tgStartParam()`, `tgChatInstance()` (только для UX, сторону
    решает сервер); `js/state.js` — `runId` генерится ТОЛЬКО в `startGame`, переживает revive;
    `js/game.js` — в `gameOver` рядом с `submitScore` добавлен `submitBattleScore(runId, score)`,
    `initBattle()` на буте, гард ближайшего препятствия дополнен; `js/config.js` — секция
    CHAT BATTLES (`BATTLE_POLL_MS`, `BATTLE_NAME_MAX`, два текста шаринга); `index.html` —
    кнопка+оверлей, кэш-бастер `?v=16`, CSS-секция BATTLE OVERLAY по образцу лидерборда.
  - **Сознательно вне MVP**: призрачный реплей (следующая волна 5.1), бот-пуши результата в
    чат, серверный анти-чит сверх санити-капа, автоназвание команды по названию чата (initData
    не содержит title/chat_id — опциональный бонус позже через добавление бота в группу).
  - **Ограничение**: `submit` после конца окна битвы → 410 (post-revive-хвост за дедлайном
    отбрасывается).

  - **Плейтест-чеклист волны 4** (нужен **второй аккаунт** — одному не проверить, uid
    фиксирует сторону навсегда):
    1. Владелец O открывает игру ИЗ группового чата «Альфа» → `⚔️` → вводит имя → «Создать
       битву» → счёт 0:0, сообщение «команда привязана».
    2. «⚔️ Вызвать врагов» → шарит ссылку в чат «Бета».
    3. Помощник H открывает ссылку из «Беты» → «Вступить» → авто-сторона B → диалог имени.
    4. H играет забег → счёт стороны B растёт у обоих в пределах ~15с (поллинг); rename после
       забега → «Уже нельзя».
    5. O играет забег с revive (смерть на очках ~100 → оживление → смерть на ~250): вклад
       стороны A должен стать ровно 250, а НЕ 350 — ключевая проверка дельта-дедупа по
       `uid:runId`.
    6. H открывает ту же ссылку из «Альфы» повторно → остаётся в стороне B (сторона навсегда).
    7. Третий аккаунт открывает ссылку из личных сообщений (не из группы) → появляется пикер
       сторон.
    8. Конец окна (дождаться либо вручную выставить `ends` в прошлое через `HSET` в консоли
       Upstash) → обе стороны видят ПОБЕДА/ПОРАЖЕНИЕ соответственно; `submit` после конца
       окна → 410; бейдж на кнопке `⚔️` гаснет.
    9. Вне Telegram кнопки `⚔️` нет вообще.
    10. Регресс: лидерборд/магазин/карта работают как прежде.

## Этап 3 — Лидерборд + бэкенд (сделано в волне 2, ждёт включения env)
Код задеплоен (см. волну 2 выше); включение — [`BACKEND_SETUP.md`](BACKEND_SETUP.md).
После включения: понаблюдать за лимитами бесплатного Upstash (10k команд/день хватит надолго)
и добавить «престиж» поверх медалей (титулы/рамки — см. идеи ниже).

Домашний Windows-ноутбук как сервер — отклонён (аптайм/безопасность/латентность), Vercel
бесплатен и деплоится тем же `git push`.

## Этап 4 — Экономика (частично сделано в волне 2)
- ~~Монеты на бревне~~ — **сделано** (риск/награда, парящие в прыжке; см. волну 2).
- ~~Аватарка на торце~~ — **сделано** («Твоё бревно», 300 🪙, премиум).
- ~~Консюмаблы~~ — **сделано**: запасное сердце (25), revive (40).
- Осталось: **палитры бревна как скины** (берёза/дуб/вишня/неон — наборы CSS-переменных,
  палитро-система делает их почти бесплатными; в `#log-svg` цвета пока прошиты — потребуется
  вынести в `--bark-*`/`--wood-*`), **скины персонажа** (после прокачки персонажа, задел
  `--char-*`), **миссии/ачивки** (следующая «морковка» по программе интервью).
- **CloudStorage-синк кошелька** — сознательно отложен (union/max для валюты = дюп монет
  между устройствами; нужен аккуратный merge или серверный кошелёк вместе с лидербордом).
- **«Престиж» для социалки** (идеи, обсудить): титулы за место в топе («Король бревна»),
  рамка/корона аватарки в лидерборде, сезонные сбросы топа с наградами.
  Минимум (медали 🥇🥈🥉 в топе) уже есть.
- ~~Открытие биомов со временем~~ — **сделано в волне 3** в новой модели: биом = мир на весь
  забег, открытие через 3 обязательных задания (не покупается за монеты; скип за донат —
  задел на будущее). «Покупка старта с позднего биома» из старой модели неактуальна —
  старт в любом ОТКРЫТОМ мире и так бесплатен с карты.
- **Порядок волн 4–6 (актуален; переигран владельцем 7 июля 2026, вечер —
  мини-ЧМ уехал в конец):**
  1. **Волна 4 — Битвы чатов** → **ЗАДЕПЛОЕНА 7 июля 2026** (см. секцию «Волна 4
     «Битвы чатов MVP»» выше — состав, ограничения MVP и плейтест-чеклист).
     Изначальный план ниже — для справки, что уже сделано и что осталось:
     пока играет только владелец, битвы дают повод звать людей и возвращаться —
     метрика ретеншна; чаты = кланы, бой по ссылке-вызову, окно 24–48ч, **счёт =
     сумма ВСЕХ очков всех участников за окно** (решение владельца: массовость и
     грайнд решают) — **сделано** (окно 24ч, сумма очков). Внутричатовая
     конкуренция параллельно — не сделано. **Призрачный реплей боя**: толпа
     синих/красных человечков с TG-аватарками, редеет по мере смертей, счёт
     команд копится — **отложено в волну 5.1**, следующую сразу за этой
     (требует запись забегов; серверные команды уже есть).
  2. **Волна 5 — Магазин 2.0** → **ЗАДЕПЛОЕН 7 июля 2026 (MVP)**: скины персонажа =
     главная витрина. Сделано: фигурка укрупнена ~1.36× (viewBox не тронут);
     переменные `--char-*`; инлайн-слои скинов по 4 слотам (голова/тело/ноги +
     полные костюмы-перекрас; костюм перекрывает индивидуальные); модель
     экипировки per-slot + миграция аватара-бревна; реестр `SKINS` (~10 позиций:
     кепка/шлем/корона, футболка/броня/плащ, сапоги/джинсы, ниндзя/золотой) с
     редкостями (рамки `--rar-*`) и ценами под грайнд; редизайн магазина
     (манекен + табы + сетка карточек); клон падения одет синхронно. Кошелёк
     остаётся ЛОКАЛЬНЫМ (по решению владельца). **Осталось (позже, НЕ MVP):**
     сундуки-рандом (гача + пул редкостей + анимация вскрытия), питомец-компаньон
     (донат/сверхредкий сундук), слоты ботинки/перчатки как отдельные (сейчас
     ноги — один слот), сезонные эксклюзивы, серверный/облачный кошелёк (иначе
     дорогие скины теряются при очистке кэша), донат-цены до Stars (заглушка =
     недостижимое число монет). **Плейтест-чеклист:** укрупнённая фигурка не
     наехала на HUD/препятствия и фил бега/прыжка/падения цел (главная проверка —
     укрупнение изолировано в свой коммит, ревертабельно); скины читаются на ходу;
     примерка/покупка/снятие + манекен; костюм перекрывает; аватар-бревно/сердце
     в «Прочем» работают; клон падения одет; регресс кошелька/revive; миграция
     старого `equippedSkin`.
  3. **Волна 6 — События**: полноформатные события ~1/мес по 4 биома; первым
     может стать футбольное (Мексика/Канада/США/Финал) или мини-версия одним
     биомом «Футбол» в секции ⚽ (слот на карте уже есть). ⚠️ Дедлайн финала
     ЧМ-2026 (~19 июля) при таком приоритете почти наверняка пропускаем —
     владелец в курсе; тогда событие на другой инфоповод или к следующему
     турниру. Сезоны/титулы — после битв.

- **Плейтест-правки после Магазина 2.0 (7 июля 2026, ЗАДЕПЛОЕНО, `?v=22`)** — владелец
  принёс список с телефона (баги/шероховатости, не новая волна), каждый пункт — свой
  коммит (точечный откат при регрессе):
  1. **Персонаж поднят над бревном**: реальная посадочная позиция задавалась НЕ базовым
     CSS `#player{top}`, а inline-стилем в `dropPlayer()` (`js/game.js`) поверх анимации
     приземления `fallDown` — поправлены оба синхронно (-25px → -34px). Точка старта
     анимации падения в воду (`render.js`, ORBIT_RADIUS-геометрия) промерена отдельно —
     расхождение ~2px, трогать не пришлось.
  2. **Кнопки старт-экрана вставали в один ряд**: у `#start-btn`/`#map-btn` не было
     явного `display` (браузер давал дефолтный inline-block), плюс `render.js` после
     конца забега сбрасывал `map-btn` на `''` вместо `'block'` как у `start-btn` рядом.
  3. **Боевой UI**: во время боя с боссом (`body.boss-fight`) прячутся 🛒/🏆/`?`, остаются
     только ⚔️ и 🔊.
  4. **Баг «иногда нет звука, чинится перезапуском»**: `tone()`/`noise()` в `js/audio.js`
     не проверяли `ctx.state` — если контекст засыпал (сворачивание/звонок) вне жестовых
     точек вызова `initAudio()`, пассивные звуки (удар/монета под наклонным управлением
     без тапов) молчали до полного релоада. Вынесен `resumeIfSuspended()`, вызывается
     перед каждым звуком + на `visibilitychange`.
  5. **Магазин**: табы стали двухуровневыми — «Скины» (под-табы Голова/Тело/Ноги/Костюмы)
     / «Сундуки» (заглушка «скоро») / «Прочее» (без изменений) — см. `shop.js` в таблице
     модулей.
  6. **Стрелка направления**: наконечник увеличен (14/7 → 20/11 у основной, превью
     пропорционально ÷1.632 для бесшовного морфа) — плохо читался.
  7. **Живой баг, пойман на телефоне ПОСЛЕ первого прохода** (не было в исходном списке):
     плашка-подсказка `#boss-hint` («ДЕРЖИСЬ!» и т.п.) зависала на экране «УПАЛ! Очки»
     после боя с боссом. Причина — видимость плашки в CSS завязана на класс `.on` у
     `#boss-layer`, а `bossHide()` (вызывается из `gameOver`) намеренно НЕ снимает `.on`
     (нужен ревайву), только добавляет `.hidden` для самого визуала босса. Потребовалось
     два коммита: сперва очистили текст в `bossHide()`, но пустая плашка (фон/рамка) всё
     ещё рисовалась — оказалось, видимость держится не на тексте, а на самом классе;
     финальный фикс — селектор `#boss-layer.on:not(.hidden) ~ #boss-hint`.
  - **Не тронуто сознательно**: баг «стрелка обещала не то направление» (гипотеза — в
    «Дизайн-заметки и известные риски» ниже, владелец сам ещё наблюдает за условиями) и
    калории — отложены на будущее по решению владельца.
  - **Что ещё стоит проверить на телефоне**: пункт 1 (не наехала ли фигура на HUD/
    препятствия) и пункт 4 (сложно симулировать сворачивание приложения вне реального
    устройства — код-ревью пройден, живого теста ещё не было).

- **Лёгкий беклог-заход (7 июля 2026, ЗАДЕПЛОЕНО, `?v=25`)** — deep-интервью после
  плейтеста v22 («в целом норм»): волну 6 «События» и Битвы 5.1 сознательно отложили,
  владелец попросил «большую волну, но только лёгкие правки». 5 коммитов:
  1. **Дев-кнопка «⚡ Ускорить конец боя»** (`api/battle.js` op `devExpire` + кнопка в
     `#bt-main` под `?dev=1`): подвигает `ends` СВОЕЙ битвы на +5 минут (только
     сокращение — не продлевает и не «воскрешает» уже завершённый бой), чтобы играть
     не дожидаясь реального 24ч окна. Мотиватор — все тестовые аккаунты владельца были
     застряли в одном бою на ~22ч, чеклист-пункт волны 4 «конец окна» было не проверить.
  2. **Бейдж «⚔️ в бою» для ВСЕХ строк лидерборда** (`api/top.js` поле `inBattle`,
     `js/leaderboard.js`): важный нюанс — `bt:u:<uid>` только поинтер, его TTL (24ч)
     обновляется и на create, и на join, так что он переживает реальный конец боя на
     часы; проверяется живой `ends`, а не только наличие поинтера. Счётчик побед в
     боях (владелец предложил как идею на будущее) — НЕ сделан: нужна событийная
     фиксация момента конца боя, сейчас считается лениво по запросу.
  3. **Калории на экране смерти** («≈N ккал», `KCAL_PER_MIN=1.0` в config.js) — лёгкая
     версия без сохранёнки/лидерборда, на лету из `state.elapsed`.
  - Статику (видимость дев-кнопки, разметку бейджа/калорий) проверили в превью
    (rsync-копия + прямые вызовы `animateFall()`/инъекция строк топа); реальный бой
    и живой лидерборд — ждут владельца на телефоне.
  - **Осталось от владельца**: нажать дев-кнопку в реальном застрявшем бою и
    досмотреть экраны победы/поражения/ничьей на обеих тестовых учётках — закрывает
    зависший чеклист-пункт волны 4.

- **Правки по фидбеку после лёгкого захода (7 июля 2026, поздно вечером, ЗАДЕПЛОЕНО,
  `?v=27`)** — владелец потестил и принёс 5 пунктов:
  1. **Баг: `#kcal-result` не чистился между забегами** — висел по центру экрана
     (`#hud-sub`) весь следующий забег. `kcalEl` не попал в списки очистки
     `showCountdown()`/`showReviveCountdown()` при добавлении в прошлой сессии —
     остальные соседи (`statusEl`/`deathQuipEl`/`runCoinsEl`/`questResultEl`) там
     были, kcal — нет. Проверено в превью: старое значение гаснет при старте.
  2. **Дев-режим недостижим из настоящего Telegram**: `?dev=1` не помогал с
     застрявшим боем, потому что battle-кнопка скрыта вне Telegram
     (`isInTelegram()`), а реальный бой живёт в localStorage именно TG-вебвью.
     Добавлен `isDevMode()` (`js/tg.js`): `?dev=1` ИЛИ `tgStartParam()==='dev'` —
     последнее достижимо изнутри настоящего Telegram через ссылку
     `t.me/StayOnLog_bot/game?startapp=dev`.
  3. **Кнопка «Коммьюнити» 📢** → канал `t.me/StayOnLog` (владелец выбрал канал,
     не чат `t.me/StayOnLogComm`). Простая ссылка без проверки подписки.
     `openTgLink()` в tg.js (нативный `openTelegramLink` внутри Telegram, иначе
     `window.open`) — по пути пойман и исправлен баг: SDK-объект существует и вне
     настоящего Telegram, `isInTelegram()` обязателен как гард, иначе
     `openTelegramLink` шлёт postEvent в никуда вместо честного фолбэка.
  4. **Стрелка** — см. «Дизайн-заметки» выше, расследование закрыто.
  5. **Находка владельца: нативный TG-ярлык на «Домой»** — см. ниже, отдельным
     пунктом (сняло архитектурный вопрос из беклога).

- **Беклог (не в этой волне)**:
  - ~~Кнопка «Коммьюнити»~~ — сделано выше.
  - **Идея владельца (8 июля 2026): меню соц. сетей вместо прямого перехода**.
    Сейчас 📢 сразу дёргает `openTgLink()` на единственную ссылку
    (`COMMUNITY_URL` = канал `t.me/StayOnLog`) и уводит игрока из игры.
    Вместо этого — открывать небольшое меню/оверлей со списком ссылок
    (канал уже есть, дальше могут добавиться другие соц. сети), чтобы
    по возможности не выкидывать игрока из мини-аппа лишний раз без
    необходимости. Сама задача — построить это меню (тот же паттерн
    оверлея, что у магазина/лидерборда/карты), не просто добавить URL.
  - ~~Ярлык на рабочий стол iPhone без потери синка с TG~~ — **вопрос снят
    (7 июля 2026)**: владелец нашёл, что Telegram сам умеет добавлять ярлык на
    экран «Домой» (меню `···` мини-аппа → «Добавить на экран Домой» → идёт через
    Safari по инструкции) — тапнув по нему, открывается НЕ голый Safari-PWA, а
    настоящая Telegram-сессия с рабочим Mini App внутри. Это нативно закрывает
    ровно ту проблему, ради которой обсуждался кастомный логин/код-привязка
    (`initData`/CloudStorage работают как обычно, никакого отдельного
    localStorage-силоса) — без единой строчки кода с нашей стороны. Кастомный
    личный кабинет остаётся актуален только для Этапа 6 (Google Play/iOS —
    там принципиально нет Telegram, эта находка их не касается).

- **Волна 4.1 «Персистентный чат» (8 июля 2026, задеплоено, `?v=28`)** — по
  фидбеку владельца: битвы были единственным режимом социалки, нужен ещё
  классический внутричатовый рейтинг, а сама механика — на постоянном
  членстве в чате, а не разовой привязке к битве (deep-интервью 2 раунда —
  просьба «второй режим боя» вскрыла более фундаментальную вещь). Не путать
  с «волной 5.1» выше (призрачный реплей боя) — другая, всё ещё не сделанная
  фича, номер совпал случайно из-за неформального обозначения в чате с
  агентом:
  - **Персистентный чат** (в игре — «чат», без слова «клан»): игрок
    закреплён максимум за ОДНИМ чатом одновременно (`chat:<ci>` в Upstash,
    ключ = сам `chat_instance` — отдельная таблица соответствий не нужна).
    Одна умная кнопка «Вступить в чат» сама решает: чат ещё никто не
    открывал/опустел — основание (спросит имя, до `CHAT_RENAME_LIMIT=3`
    переименований), чат уже есть — обычное вступление. Выход разрешён
    всегда, КРОМЕ момента активной межчатовой битвы своего чата (простой
    антиабьюз против «перебежать к лидеру посреди боя»); выход теряет
    накопленный вклад — осознанно.
  - **Внутричатовый рейтинг** — НЕ матч с окном, перманентный зачёт вклада
    среди участников одного чата (та же механика contrib-таблицы, что у
    битв, только без `ends`).
  - **Межчатовая битва** (то, что было раньше) не изменилась по сути — всё
    ещё разовое окно 24ч — но стороны A/B теперь не одноразовая привязка
    `chat_instance` на момент создания, а два ЦЕЛЫХ постоянных чата: сторона
    определяется на лету (`chat:u:<uid>` игрока против `chatA`/`chatB`
    битвы), поэтому каждый участник чата автоматически «в бою», как только
    его чат — сторона, без отдельного тапа «вступить в битву». Имя команды —
    снимок имени чата на момент создания/принятия (не живая ссылка).
  - **Полный гейт**: без чата ни один из двух режимов не открывается; ⚔️
    остаётся единственной точкой входа (верхняя панель и так не резиновая).
  - **Сервер**: `api/battle.js` — новые `chatJoinOrFound/chatLeave/
    chatRename/chatState`, старые `create/join/state/submit/devExpire`
    переписаны на членство в чате; `bt:<id>:members` и `opRename` (для
    команды боя) убраны целиком — переименование команды больше не
    отдельная операция, это переименование чата. `api/top.js` — бейдж
    «⚔️ в бою» на общем лидерборде теперь резолвится через
    `chat:u → chat:<ci>:battle → bt:<id>.ends` вместо снятого `bt:u:<uid>`
    (сам бейдж не изменился ни на пиксель — только источник данных).
  - **Сознательно вне волны**: принадлежность к чату в общем топ-10
    (`api/top.js`) — не потому что технически сложно (паттерн джойна для
    этого уже есть — тот же, что у бейджа «в бою»), а потому что `lb`
    хранит рекорд ЗА ВСЁ ВРЕМЯ без даты, а чат — факт СЕЙЧАС; пара «этот
    рекорд — этого чата» может быть неправдой. Вернёмся к этому вместе с
    будущим профилем/престижем, когда появится модель «очки, заработанные
    для чата».
  - **Живой баг, пойманный в превью до пуша**: `#chat-battle-panel` получил
    CSS-дефолт `display:none` (от вспышки «и панель, и кнопка вызова видны»
    на самом первом открытии), но код показывал его через
    `style.display = ''` — а это больше не значит «показать», раз есть
    дефолт в стилях, значит «взять дефолт» (т.е. остаться скрытым). Поймано
    прямо в превью (форсировал состояние «бой активен» — панель не
    появилась), едва не ушло в пуш с невидимой боевой панелью. Оба места
    правлены на явный `'block'`.
  - **Плейтест-чеклист** (нужен **второй аккаунт**, как и у волны 4):
    1. Открыть игру из чата «Альфа» → «Вступить в чат» → первый → назвать →
       видно в перманентном рейтинге.
    2. Второй аккаунт из ТОГО ЖЕ чата «Альфа» → «Вступить в чат» → обычное
       вступление, видит того же первого в рейтинге.
    3. Оба играют — рейтинг растёт у обоих без окна 24ч, переживает
       перезапуск.
    4. «Вызвать другой чат» → шарит ссылку в чат «Бета» → второй аккаунт
       (ещё без чата) открывает → видит экран «Вступить в чат» (не сразу
       битву) → вступает/основывает «Бету» → автоматически доигрывается
       принятие вызова.
    5. Оба играют — счёт битвы растёт у ОБОИХ участников каждого чата, без
       отдельного «вступить в битву» с каждой стороны.
    6. Попытка выйти из чата «Альфа» посреди битвы → отказ.
    7. Конец окна (или дев-кнопка ускорения) → победа/поражение/ничья
       видны — включая при повторном заходе ПОСЛЕ конца окна (не только
       если смотреть на таймер живьём).
    8. Регресс: общий лидерборд + бейдж «в бою», магазин, карта, шаринг.

  - **Правки по живому фидбеку от владельца (8 июля 2026, тот же день)**:
    1. **Баг**: клик «Вступить в чат» без группового чата в контексте (Home
       Screen-ярлык, личка с ботом) показывал жёлтое «Вступаю…» на секунду
       и ничего — не зависание, а `showState('none')` после `setMsg(...)`
       молча стирал текст (`showState` сам чистит сообщение первым шагом;
       если экран не меняется, стирать нечем заменить). Порядок исправлен
       в обоих местах (`joinOrFoundChat`, `joinBattle`) — сообщение
       выставляется ПОСЛЕ перехода, а не до.
    2. **Продуктовый гэп, найденный владельцем**: если открыть игру не из
       группового чата вообще (в том числе с ярлыка «Домой»), кнопка
       «Вступить в чат» была тупиком — сказать «открой из чата» и всё,
       без способа туда попасть прямо из игры. Первая версия фикса
       (отдельная кнопка «Отправить ссылку», видна только после ошибки)
       владелец отклонил — попросил сделать ровно ОДНУ кнопку «Вступить в
       чат» адаптивной: если игра открыта из чата — обычный флоу; если нет —
       сразу шарит ссылку `SHARE_URL?startapp=joinchat`, без отдельного
       экрана-ошибки. Решение — клиентская проверка `tgChatInstance()` (уже
       существовала в tg.js как «UX-хинт», просто не использовалась здесь)
       ДО обращения к серверу: есть контекст — `joinOrFoundChat()` как
       раньше; нет — сразу `shareChatInvite()`. Открытие расшаренной ссылки
       уже из нужного чата на буте автоматически повторяет попытку
       вступления (без дополнительного тапа), тем же паттерном, что уже
       работает для вызовов на битву (`startapp=b_<id>`).
    3. **Серьёзный баг, найденный владельцем на реальном телефоне**:
       перманентный внутричатовый рейтинг не показывал НИКОГО — потому что
       `chat:<ci>:contrib` инкрементился только внутри `opSubmit`
       (эндпоинт битвы), а тот вызывается ТОЛЬКО пока активна межчатовая
       битва. Обычный забег вне боя не писал вообще ничего — ровно
       противоположность задуманному «перманентный зачёт, без окна 24ч».
       Исправлено: новый независимый `opChatSubmit` (свой дедуп по
       `chat:<ci>:runs`, отдельный от `bt:<id>:runs`) вызывается на КАЖДОМ
       завершении забега (`submitChatScore` в `js/game.js`, рядом с
       `submitScore`/`submitBattleScore`), вне зависимости от того, идёт
       ли сейчас битва; инкремент вклада в чат убран из `opSubmit`
       полностью (иначе задвоение во время боя).
    4. **Мелкая полировка по фидбеку**: заголовок оверлея «Битва чатов» →
       «Битвы»; ссылки «переименовать чат»/«Покинуть чат» укорочены до
       «Переименовать»/«Покинуть»; кнопка «Вызвать другой чат» — свой
       оранжевый цвет (`#chat-challenge-btn`) вместо общего зелёного
       `.bt-primary` (тот делят почти все остальные кнопки оверлея, трогать
       общий класс было нельзя). Кэш-бастер `?v=29`.
    5. **Баг, найденный владельцем сразу после фикса #3**: все участники в
       внутричатовом рейтинге отображались как «Игрок» — `chat:<ci>:names`
       был в схеме и читался в `opChatState`, но ни один оп никогда его не
       ПИСАЛ (в отличие от `bt:<id>:names`, который `opCreate`/`opJoin`
       честно заполняют). Дописано в трёх местах: `opChatJoinOrFound`
       (и на основании/вступлении, и в идемпотентной ветке «уже в этом
       чате» — чтобы не требовать выйти-зайти), и в `opChatSubmit` (пишется
       при каждом забеге — самое надёжное место, бэкфиллит имя даже тем,
       кто вступил ДО этого фикса, без выхода из чата).

- **Начало теста на друзьях (8 июля 2026, состояние на коммите `eabe372`)**:
  с этой точки владелец начинает показывать игру друзьям и собирать
  обратную связь от них — не только от собственного плейтеста. Дальше
  ожидается много некрупных правок по неструктурированному, живому фидбеку
  (не по заранее спланированным волнам) — реагировать на конкретные
  жалобы/баг-репорты как они приходят, а не пытаться уложить их в одну
  большую волну.

## Этап 5 — PWA (P2)
- `manifest.json`, иконки, service worker (cache-first статика, network-first api), офлайн-игра.
- Предусловие Этапа 6.

## Этап 6 — Магазины приложений (P2-P3)
- **Google Play**: TWA через Bubblewrap (нужен готовый PWA + `assetlinks.json`).
- **iOS**: Capacitor-обёртка (аккуратно с motion-permission в WKWebView).
- ~~Блокер качества: нормализация по delta-time~~ — **сделано** вместе с этапом 2
  (см. «Уже сделано»): темп теперь одинаков на 60/90/120 Гц.

## Этап 7 — Контент-лонгтейл (P3)
- Биомы, влияющие на физику (шторм — качка `game-container`), пауэр-апы (щит/слоу-мо/магнит),
  ежедневные челленджи.
- **Новые биомы-темы** — теперь это карточки карты кампании (реестр `BIOMES`/`CHAPTERS` в
  `js/biomes.js`): новый мир = dayPalette (5 палитр цикла) + world-класс (перекрас
  бревна/препятствий CSS-only) + 5 мудов музыки + 3 задания + босс со своим типом боя.
  Типы боёв уже в движке: паттерн, погоня; дальше — контратака и новые.
  - ~~Снежный~~ — **сделан в волне 3 (Зимний)**: лёд, сосульки, косатка-погоня.
  - **Лавовый**: лава вместо воды (падение = сгорание, не всплеск — свой `render.js`-эффект),
    препятствия — горячие камни/угли.
  - **Цифровой**: вместо бревна — биткоин (крутящаяся монета вместо коры-цилиндра, самый
    смелый арт-своп из всех), внизу вместо воды — опенспейс со старыми большими компьютерами,
    по фону ходят люди в очках и белых сорочках (декоративный параллакс-слой), препятствия —
    компьютерные мышки. Самый затратный по арту биом — кандидат делать последним/как ивент.
  Порядок появления и правила разблокировки — см. «Открытие биомов со временем» в Этапе 4.

## Дизайн-заметки и известные риски
- **Контроль-фил** — корень удержания первой сессии; тюнинг в `config.js` + слайдерах `?dev=1`.
  `js/input.js` не трогать без крайней необходимости.
- **Отложенная боль (решение владельца, 7 июля 2026): инпут на поздних фазах** —
  «при резком реверсе попытки выровнять руками ни к чему не приводят», при этом на
  клавиатуре (десктоп) играть ЗНАЧИТЕЛЬНО проще, чем наклоном телефона → разгадка
  в штурвале-акселерометре (EMA/чувствительность/потолок скорости — диагностировать
  сравнением с клавиатурным инпутом). Чинить В ПОСЛЕДНЮЮ очередь: «игра должна быть
  хоть в чём-то сложной»; сначала контент и социалка (волны 4–6).
- Прыжок: тайминги/keyframes `playerJump` — зона откатов (см. git log d2346f4/9a70040).
- ~~Открытое расследование: «бревно сменяет направление не в ту сторону, куда
  обещала стрелка»~~ — **ИСПРАВЛЕНО (7 июля 2026, поздно вечером)**: владелец
  подтвердил паттерн воспроизведения («подмена всегда сопровождалась появлением
  препятствия»), что в точности совпало с гипотезой. Причина: `reconcilePendingChange()`
  (`js/game.js`) вызывался только на входе в варнинг-окно + финальным гардом в
  момент срабатывания — если препятствие всплывало ПОСЕРЕДИНЕ 1.5с варнинга (свой,
  не связанный с этим таймер), превью-стрелка молча держала обещанный реверс до
  самого промоушен-морфа, который внезапно оказывался чистой сменой скорости.
  Фикс: reconcile теперь гоняется каждый кадр, пока варнинг активен, и
  перерисовывает превью СРАЗУ при конвертации — до промоушена, а не в его момент.
  Финальный гард в `applyPendingChange()` не тронут (гарантия честности на грани
  кадра срабатывания — геймплейная, не визуальная). Живой тест на телефоне ещё
  предстоит (таймер препятствий не крутится в песочнице).
- Плейтестом проверить после Этапа 1: контраст сучков на всех 4 биомах, честность ветки
  (окно 80мс с обеих сторон прыжка), гэп двойного, плотность препятствий в фазе 3,
  позицию пилюли/стрелки-подсказки на маленьких экранах.
- Плейтестом проверить после Этапа 1.2: не стал ли assist «играет само» (фаза 1 = 30%),
  читаемость виньетки на шторме (красное на тёмном), не надоедает ли музыка за 2-3 минуты
  и её баланс громкости с SFX, ощущаются ли новые хаптики на Android и в TG на iOS.
- Плейтестом проверить после Этапа 2: подхват ника из TG-профиля (внутри Telegram оверлей ника
  не должен появляться); кнопка «Похвастаться» в TG (открывается шит выбора чата), в Safari/Chrome
  на телефоне (системный share sheet); клавиатурный режим в десктоп-браузере; и главное —
  **изменился ли темп игры после dt-нормализации**: на 60 Гц экране — не должен, на 90/120 Гц
  игра станет медленнее (= честнее); тогда тюнить скорости в config.js.
- Плейтестом проверить после Волны 1: **схема B** (в `?dev=1` включить `tilt`) — фаново ли,
  не дрейфует ли нейтраль, хватает ли `tilt rate` на фазе 3; **туториал** — доводит ли до
  «понял» за ~минуту, не бесит ли повтор баланса после падения, заметна ли кнопка «Пропустить»;
  **баннеры** — читаемы ли на шторме; сам туториал сработает один раз (дальше — `?tut=1`).
- **Плейтестом проверить после Волны 2** (чеклист на один заход):
  1. Стрелки на бревне: читается ли «длина = скорость», понятно ли превью, не мельтешит ли
     промоушен; варнинг-мигание заметно? Пилюли снизу больше нет — чего-то не хватает?
  2. Туториал: новый шаг стрелки (`?tut=1`) — понятен ли с одного раза; музыка играет?
  3. Поздняя игра (задержаться за 200+ очков): бревно всё ещё «бешеное» или ок?
     Реверс теперь с провалом скорости — чувствуется мягче?
  4. Монеты: заметны ли, приятен ли сбор (звук/вибра/попап), не мешают ли препятствиям;
     парящую реально ли взять; доходность за забег (~8–15?).
  5. Магазин: купить сердце → следующий забег с 3 сердцами; аватар-бревно (в TG с фото
     профиля) — купить, посмотреть на вращении, снять/надеть.
  6. Revive: умереть с 40+ 🪙 → «⚡ Продолжить» → счёт/биом сохранены, щит работает,
     второй раз за забег кнопки нет.
  7. Смерть: квипы забавные? Не повторяются ли слишком часто?
  8. Лидерборд (после включения env по BACKEND_SETUP.md): кнопка 🏆 появилась, свой счёт
     в топе, имя из TG корректное.
- **iOS-пермишен на датчики** — потенциальная точка оттока в воронке.

## Рамки
Ваниль JS, **без сборки**. Не переписывать на фреймворк. Мобильный перформанс, ограничения
акселерометра/WebView, политика автоплея звука (старт только по жесту).
